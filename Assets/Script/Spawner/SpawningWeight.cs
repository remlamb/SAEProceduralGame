using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawningWeight : MonoBehaviour
{
    // Start is called before the first frame update
    public float weight;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
